<%= simple_form_for(admin) do |f| %>

  <%= f.input :username %>
  <%= f.input :email %>
  <% if current_admin.superadmin %>
    <%= f.input :password %>
  <% end %>
  <%= f.button :submit %>

<% end %>
