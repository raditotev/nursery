<%= form_for(testemonial) do |f| %>
  <% if testemonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testemonial.errors.count, "error") %> prohibited this testemonial from being saved:</h2>

      <ul>
      <% testemonial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
