alert("Thanks for your request! We'll be in touch shortly.");
$("body").attr("class", "");
$("body").attr("style", "");
$("#requestTour").attr({
  "class" : "modal fade",
  "style" : "display:none;"
});
$("form")[0].reset();
$(".modal-backdrop.in").remove();
