<%= form_for(closure) do |f| %>
  <% if closure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(closure.errors.count, "error") %> prohibited this Break from being saved:</h2>

      <ul>
      <% closure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %>
    <%= f.date_select :start_date %>
  </div>

  <div class="field">
    <%= f.label :end_date %>
    <%= f.date_select :end_date %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
